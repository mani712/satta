/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.html"], // scans all .html files in the root folder
  theme: {
    extend: {}, // for custom theme extension
  },
  plugins: [], // add plugins like forms/typography here
}